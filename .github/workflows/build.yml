# Java / Maven
target/
*.class
*.war

# 允许提交 libs 里的小 jar（PlaceholderAPI / Vault）
# 但排除大文件（paper 30MB）
libs/*.jar
!libs/PlaceholderAPI-2.9.2.jar
!libs/Vault.jar
!libs/paper-1.12.2.jar.dummy

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# 日志
*.log
logs/

# 系统
.DS_Store
Thumbs.db

# 敏感
config.local.yml
*.local.yml
secrets.yml

# 运行数据
data/
core-data/
配置/
